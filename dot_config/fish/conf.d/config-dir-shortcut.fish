# Shell config directory shortcut — same alias name as bash/zsh's `brc`, but
# points at fish's own config directory (user's explicit 2026-08-18 request:
# universal alias name, per-shell target).
abbr -a -- brc 'cd ~/.config/fish'
