{{- /*
  Rendered by chezmoi from .chezmoidata/env/*.yaml — do not edit this file
  directly, edit the source YAML instead (see CONCEPT_ROADMAP.md §4).
  Shared by bash and zsh (see §1/§3.3) — sourced by both via their respective
  rc files. `$'...'` ANSI-C quoting handles both plain values and ones with
  `\e` escape sequences (e.g. LESS_TERMCAP_* man-page colors) identically.
*/ -}}
#!/usr/bin/env sh
{{ range $cat, $entries := .env.common.categories }}
# {{ $cat }}
{{- range $entries }}
{{- $val := includeTemplate "resolve-os-value" (dict "value" .value "os" $.chezmoi.os) | trim }}
{{- if ne $val "skip" }}
export {{ .name }}=$'{{ $val }}'  # {{ .description }}
{{- end }}
{{- end }}
{{ end -}}
